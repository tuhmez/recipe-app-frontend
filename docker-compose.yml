version: '3.7'

services:
  recipe-app-frontend:
    build: .
    image: recipe-app-frontend:latest
    container_name: recipe-app-frontend
    ports:
      - 3000:3000
    environment:
      - SERVER_ADDRESS=localhost
      - SERVER_PORT=3001
    restart: always
networks:
  default:
    name: recipe
